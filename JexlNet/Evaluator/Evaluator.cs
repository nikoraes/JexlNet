namespace JexlNet;

public class Evaluator
{
    /* private Dictionary<string, Func<dynamic, Task<dynamic>>> _handlers;
    public Grammar grammar { get; set; };
    private Dictionary<string, dynamic> _context;
    private Dictionary<string, dynamic> _relContext;
    private Task _promise;


    public async Task<dynamic> EvalAsync(Node ast)
    {
        await _promise.ConfigureAwait(true);
        EvaluatorHandlers.Handlers.TryGetValue(ast.Type, out var handleFunc);
        return 
        
        await EvalAsync(ast, _context).ConfigureAwait(true);
    } */
}